class User {
    constructor(UID,first,last,communities,category) {
      this.uid = UID;
      this.first = first;
      this.last = last;
      this.communities = communities;
      this.category = category;
    }
  }